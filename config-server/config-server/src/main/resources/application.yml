server:
  port: 9595

spring:
  application:
     name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/halappa-done/Demo
          clone-on-start: true

